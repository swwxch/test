/**
 * Created by zhaofs on 2017/5/30.
 * 系统模块导出
 */

import handlerManager from "../core/handler/HandlerManager";
handlerManager.dynamicImport(async function (path) {
    try {
        return await import(`../${path}/Handler.js`);
    } catch (err) {
        //console.error("load handler error");
        //return new Error(err);
    }
});