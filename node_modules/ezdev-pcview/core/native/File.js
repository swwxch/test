/**
 * Created by zhaofs on 2017/5/6.
 * --------------------
 * - 本地调用封装接口 -
 * --------------------
 * 实现本地调用封装接口
 */
export default {
    /**
     * 执行文件
     * @param file
     * @returns {*}
     */
    executeExeFile: function (file) {
        if (window.NativeFile) {
            return NativeFile.execute(file);
        } else {
            layer.alert('请在专用浏览器下使用此功能');
        }
    },


    /**
     * 查找文件
     * @returns {Array}
     */
    seachExeFile: function (file) {
        if(window.NativeFile){
            return NativeFile.search(file);
        }else{
            layer.alert('请在专用浏览器下使用此功能');
        }
    },

    /**
     * 打开对话框
     * @param callback
     */
    openFileDialog:function (callback) {
        if(window.NativeFile){
            window._OpenFileDialogCallBack=callback;
            NativeFile.openFileDialog();
        }else{
            layer.alert('请在专用浏览器下使用此功能');
        }
    }
};