/**
 * Created by zhaofs on 2016/7/21.
 * --------------------
 * - 分析器基本类 -
 * --------------------
 * 实现各种自定义属性分析，主要时对页面片段加载完成后对html元素的预处理
 */
import Events from "../../base/Events";
import analyzerManager from "./AnalyzerManager";

export default class extends Events {

    constructor() {
        super();
        this._name = "BaseAnalyzer";
    }

    /**
     * 对元素分析器进行初始化
     */
    initAnalyzer() {

    }

    /**
     * 对元素进行分析
     * @param content
     */
    analyze(content) {
    }


    get name() {
        return this._name;
    }

    set name(value) {
        this._name = value;
        analyzerManager.add(this);
    }
};